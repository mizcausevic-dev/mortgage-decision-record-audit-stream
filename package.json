{
  "name": "mortgage-decision-record-audit-stream",
  "version": "0.1.0",
  "description": "PropTech audit-stream profile: per-mortgage-application AI tool access events hash-chained for ECOA / RESPA / Fair Housing / HMDA / GLBA evidence.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:examples": "node src/build-examples.mjs",
    "verify": "node src/verify.mjs examples/pacific-coast-2026q3-stream.ndjson",
    "test": "npm run build:examples && npm run verify"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1"
  }
}
